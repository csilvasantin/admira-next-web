.claude/
.wrangler/
.runtime/
.codex-work/
tools/notebooklm-local/node_modules/

# La firma de cada release se GENERA al publicar (deploy.sh y el flujo de CI)
# a partir de release-signature.json + el commit real. Versionarla la congelaba:
# producción llegó a servir la firma de la r8 con la portada en la r18.
version.json

# dependencias locales de tests
/node_modules/
